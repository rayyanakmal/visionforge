__pycache__/
*.pyc
.venv/
.uv/
data/images/
data/*.zip
data/instances_val2017.json
.pytest_cache/

# Agent context — never commit (local-only working agreement)
AGENTS.md

# Build artifacts — never commit
*.egg-info/

# CLI output artifacts — never commit (generated by evaluate/compare runs)
report.json
report.md
compare.json
compare.md

# Streamlit secrets — never commit
.streamlit/secrets.toml
